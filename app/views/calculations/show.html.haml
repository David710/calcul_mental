%div{ id: set_content_id( @answer, @good_result ) }
  .calculs
    -# = image_tag("green-balls.png", size: "40 x 40", alt: "delete")
    .row.center-xs.center-sm.center-md.center-lg
      .col-xs.col-sm.col-md.col-lg
        #operation
          = @operation
    .row.center-xs.center-sm.center-md.center-lg
      .col-xs.col-sm.col-md.col-lg
        %p
          = form_tag do
            = text_field_tag(:result, nil, { autocomplete: "off", autofocus: "true", id: "answer-input" } )
            = hidden_field_tag(:good_result, value = @result)

  .results
    .row.center-xs.center-sm.center-md.center-lg
      .col-xs.col-sm.col-md.col-lg
        -# = render partial: "results", locals: { answer: @answer, good_result: @good_result }

- now = Time.current
- beg = Training.last(2)[0].begining
-# %p.temps temps:
-# %p.temps= ( now - beg ).round(1)

#keyboard
  #keyboard-0
    .number#zero 0
    .number#delete= image_tag("fleche.png", class: "img-number", alt: "delete")
    .number#enter= image_tag("tick.png", class: "img-number", alt: "delete")

  #keyboard-1
    .number#one 1
    .number#two 2
    .number#three 3

  #keyboard-2
    .number#four 4
    .number#five 5
    .number#six 6

  #keyboard-3
    .number#seven 7
    .number#height 8
    .number#nine 9
